#include <stdio.h>

int main(void) {
   int n;
   scanf("%d", &n);
   polje[n];

   for (int i = 0; i < n; i++) {
      scanf("%d", &polje[n]);
   }

   for(int i = 0; i < n; i++) {
      
   }

   return 0;
}